<!DOCTYPE html>
<html>
  <head>
    <title>SmartShopping</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "custom", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-dark">
      <h2 class="text-white">Smart Shop</h2>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto ">
          <li class="nav-item active  ">
            <%= link_to "Home" , root_path , class:"text-white no-underline"%>
          </li>
        </ul>
        <% if user_signed_in? %>
          <%= link_to "Sign Out" ,destroy_user_session_path ,data: {turbo_method: :delete,turbo_confirm: "Are you sure ?"} , class:"btn btn-outline-success my-2 my-sm-0"%>
        <% else %>
          <%= link_to "Sign In" , new_user_session_path , class:"btn btn-outline-success my-2 my-sm-0 float-right"%>
          <%= link_to "Sign Up" , new_user_registration_path , class:"btn btn-outline-success my-2 my-sm-0"%>
        <%end%>
      </div>
    </nav>
    <%= yield %>
    <div class="container  ">
      <div class="row justify-content-center">
        <div class="col-md-3 mrvalidate-custom"></div>
        <div class="col-md-6 ">
          <div class="form-group ">
            <% flash.each do |key, value| %>
              <% if key == "notice" %>
                <div class="alert alert-success w-custom transitions" id="show" >
                  <%= value %>
                </div>
              <% elsif key == "alert" %>
                <div class="alert alert-danger w-custom mt-4 transitions" id="show">
                  <%= value  %>
                </div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </body>
  <script>
    setTimeout(() => {
      show.style.display="none";

    }, 5000);
  </script>
</html>
